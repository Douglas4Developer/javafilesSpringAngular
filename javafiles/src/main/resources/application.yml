spring:
  datasource:
    username: root
    password: 326598
    url: jdbc:mysql://localhost:3306/javafiles_db
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

base:
  url: "http://localhost:8080"